version: 2
jobs:
  build:
    docker:
      - image: haskell:7.10.3
    branches:
      only: develop
    steps:
      - checkout

      - restore_cache:
          key: cache

      - save_cache:
          paths:
            - ~/.stack
          key: cache

      - run: stack setup
      - run: stack build
      - run: stack exec blog build 